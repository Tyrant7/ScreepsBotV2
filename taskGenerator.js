/**
 * An interface class for different TaskGenerator types. 
 * Each inheriting class must implement the run method.
 */
class TaskGenerator {
 
    run(roomInfo) {
        // Intentionally blank
    }
}