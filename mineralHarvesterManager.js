const CreepManager = require("creepManager");
const Task = require("task");

class MineralHarvesterManager {
    createTask(creep, roomInfo) {

        
    }
}

module.exports = MineralHarvesterManager;